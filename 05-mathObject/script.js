let x = 3.21;
let y = 4;
let z;

// z = Math.floor(x); // Membulatkan kebawah
// z = Math.ceil(x); // Membulatkan keatas
// z = Math.round(x); // Membulatkan ke angka bulat terdekat
// z = Math.random(); // Menghasilkan angak random 0 - 0.99
// z = Math.abs(x) // Menghilangkan tanda mines, jadi angkanya berubah plus
z = Math.pow(y, y) // Angka pertama berpangkat angka kedua
let max = Math.max(x, y, z); // Mencari angka terbesar
let min = Math.min(x, y, z); // Mencari angka terkecil

console.log(max)
console.log(min)